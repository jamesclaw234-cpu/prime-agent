# Changelog

All notable changes to this package are documented in this file.

## [Unreleased]

- Fixed the detector planning cycles the executor was guaranteed to refuse: the per-symbol widened freshness window applied to detection and sizing but the executor still gated every leg on the strict base window, so a thin-venue cycle admitted on purpose would fill leg 1 and then be refused at leg 2 - a forced unwind paying fees and spread twice. The executor now uses the same window; the IOC limit price is what actually bounds a moved book.
- Fixed the quote-skew check cancelling the widened window for any cycle mixing an active leg with a thin one - i.e. almost every cycle the widening exists to admit. A thin symbol's `receivedAt` tracks its own last change, not market time, so skew is now measured only among legs inside the base window, whose timestamps do track market time. Uniformly-thin legs are vouched for by their own cadence windows.
- Fixed the second-generation universe-cap bug: connectivity ranking tied every dual-quoted asset and the tiebreak grouped markets by quote, so a binding cap kept one leg of each asset's pair - all of which dead-end pruning deleted. In a two-quote universe the entire selection pruned to zero cycles and the bot silently never traded. The cap now admits whole per-asset market groups, hubs first, and never wastes a slot on a market that cannot survive pruning.
- Fixed the `doctor` order probe pricing at bid*0.7 unconditionally. Stablecoin bridges - the likely probe target under the universe ranking - carry tight price bands (real USDTUSD: `minPrice` 0.80, `bidMultiplierDown` 0.8), where the unclamped probe drew `-1013` and `doctor` reported FAIL on a healthy account. The probe now clamps above the symbol's own floors, stays below the touch, and pre-checks itself with the executor's validator.
- Fixed the main unwind path reporting an unsellable below-minimum sliver as stranded inventory, which - with `haltOnStranded` on - stopped the whole bot over an amount no order can sell. The identical sliver via the residual path was already classified as dust; both paths now agree.
- Fixed `replay` silently omitting `maxBookAgeCeilingMs` and `maxQuoteSkewMs`, so a recording re-run to evaluate either knob showed no effect and no longer matched the live code path.
- Hardened the loopback exchange where it was more permissive than Binance: `POST /api/v3/order` and `/order/test` now enforce PRICE_FILTER, LOT_SIZE and NOTIONAL; the funds check runs at placement against limit x origQty (Binance locks funds even for an order that will not match); order lookup is scoped to the mandatory `symbol` parameter, which the ambiguous-order reconciliation depends on; oversized `recvWindow` is refused with `-1131`; and the advertised commission rates derive from the configured fee instead of a hardcoded 10bps.
- Fixed the rate-limit diagnostic never being able to name the RAW_REQUESTS window - the one budget every request consumes - and made the two new detection keys reject NaN. Bounded `bestByCycle` against sweep-synthesized cycle ids over a 24/7 run, and the pre-fee edge line now uses the configured cycle length instead of assuming three legs.

- Added `@earendil-works/pi-btc-arb`, a Binance Spot triangular arbitrage scanner and execution engine that paper trades by default and gates live order placement behind an explicit four-condition check.
- Added a sharded `bookTicker` WebSocket feed with per-shard jittered reconnect, a data-staleness watchdog, and proactive recycling ahead of the exchange's 24-hour disconnect.
- Added exact fixed-point decimal arithmetic for all money math, with a float screening pass on the hot path that is re-verified exactly before any order is sent.
- Added depth-bounded, lot-rounded, filter-checked cycle sizing that re-derives the net edge after rounding and rejects cycles that lose money at their limit price.
- Added a leg-by-leg executor that re-sizes each leg from the previous fill net of commission, chooses between finishing and reversing a cycle, and unwinds residual inventory.
- Added a paper execution engine that models latency, partial fills, adverse selection and outright misses, seeded for reproducible runs.
- Added risk controls: kill-switch file, notional and concurrency caps, daily loss and cycle limits, consecutive-failure halt, error-rate breaker, per-symbol cooldown, clock-skew and stale-data guards, and an order-rate budget.
- Added a weight-aware rate limiter that adopts the exchange's published limits at startup and honours `X-MBX-USED-WEIGHT` and `Retry-After`.
- Added a JSONL trade ledger, metrics with latency percentiles, a live terminal dashboard, and tick recording with offline replay.
- Added the `run`, `scan`, `symbols`, `doctor`, `replay` and `config` CLI commands.
- Added recovery from an ambiguous order failure: the order is looked up by client id, and only an unanswerable lookup halts for manual reconciliation.
- Added `expiryReason` to the trade ledger, separating an ordinary lost race from a price the exchange refused under its Price Range execution rule.
- Added handling for the `serverShutdown` stream event, reconnecting on the notice instead of waiting for the socket to drop.
- Changed rate-limit header parsing to match the published `(intervalNum)(intervalLetter)` format rather than a fixed suffix.
- Changed order placement to use its own timeout above the exchange's 10-second processing timeout, so a slow order reports its outcome instead of becoming an unknown.
- Fixed `-1006` not being treated as an ambiguous outcome despite the spec stating its execution status is unknown.
- Changed the unfilled-order budget to follow the exchange's own count in both directions, since a filled order decrements it and the previous floor-only behaviour throttled the bot exactly when its orders were trading.
- Added a `binance-us.config.json` profile for users geo-blocked from Binance.com global, with the API compatibility and the missing-testnet caveat documented.
- Fixed `paper.startingBalances` rejecting any asset not present in the defaults, which turned starting from a non-USDT balance into an "unknown config key" error.
- Fixed the taker fee understating the real cost: Binance charges standard, tax and special commission components, and adds the side rate to the taker rate within each. The bot now sums all three from `GET /api/v3/account/commission` and warns when non-standard components apply.
- Added a loopback integration suite that runs the bot against a Binance Spot server speaking the real wire protocol over a real socket, using Node's own `fetch` and `WebSocket`. The server verifies the HMAC over the exact bytes it receives, enforces `recvWindow`, keeps balances and matches IOC orders, so signing and RFC 6455 framing are covered rather than assumed.
- Added `test/mock-exchange.ts` and a `mock.config.json` profile, which run that server standalone so `doctor`, `scan` and a full `run --live` can be rehearsed end to end without reaching the exchange - the only way to exercise the live order path from a geo-blocked jurisdiction.
- Fixed an untradeable partial-fill remainder being retried three times and then logged at `error` as stranded inventory. Commission is deducted from the asset received, so a sliver below the next symbol's `minQty` is the normal end state of a healthy cycle; reporting it as a failure buried the log line that means a real position is unhedged. Such a remainder is now identified before the retries, recorded as dust on the cycle's log line, and left alone.
- Documented that lot-step dust, not the fee, is the dominant cost of a small cycle. Each intermediate asset's remainder is bounded by one `stepSize` of the *next* symbol, so a fixed amount is left behind per cycle: measured at ~42bps on a $100 cycle and ~0.4bps on a $10,000 one.
- Changed a rate-limit wait that exhausts a request's budget to name the window that ran out and its usage, rather than reporting only that the request was rate limited.
- Changed `test/mock-exchange.ts` to report a port collision clearly, since a leftover server from an earlier run otherwise keeps answering while the new one dies in the background.
- Changed the Binance.US profile's fallback `fees.takerBps` from 10 to 2. Binance.US charges 0% maker and 0.02% taker on all spot pairs as of April 2026, so a three-leg cycle costs about 6bps rather than 30. Combined with the `-1101` bug below - which stopped `autoDetect` reading the real rate at all - every measured edge was reported roughly 24bps worse than reality.
- Fixed `GET /api/v3/account` sending `omitZeroBalances`, which Binance.com ignores but Binance.US rejects outright with `-1101 Too many parameters`. That endpoint is the only source of balances, so on Binance.US every refresh failed, no cycle could be funded, and a live run would have traded nothing while reporting no fault. The parameter is gone and the loopback server now refuses extras on that path so it cannot come back.
- Added the pre-fee edge and a timestamp to the edge report. The fee is a constant, so stripping it back out separates a market that is mispriced but too expensive to trade from one that is simply not mispriced - only the first is worth pursuing, and they are indistinguishable in the net figure.
- Added an edge distribution to the `run`/`scan` summary: how many cycles were actually priced, how many were skipped on a stale book, the best edge seen, a histogram by bps band, and the best each cycle reached. A run that clears nothing previously reported only zeroes, which could not distinguish a market a few bps away from one that is nowhere near.
- Fixed `universe.maxSymbols` truncating the market list alphabetically. On a USD/USDT venue the markets that make stablecoin cycles possible - `USDCUSD`, `USDCUSDT`, `USDTUSD` - sort to the very end of the alphabet, so a binding cap deleted exactly the routes most worth watching and left a table of leaf pairs. Markets are now ranked by the *less* connected of their two assets, since sitting on a cycle requires being able to leave whatever you arrive at, and a binding cap is reported rather than applied silently. A Binance.US scan saw 12 cycles where 60 existed.
- Changed the Binance.US profile's `universe.maxSymbols` from 200 to 400; the venue lists 266 markets, so the old cap was binding.
- Added `detection.maxQuoteSkewMs`, which rejects a cycle whose freshest and stalest quote are too far apart. Age alone cannot catch this: any window wide enough to admit a 4s-old quote also admits a 10ms-old one, and the pair describes a market that never existed at any single instant - the apparent edge is usually just the newer leg having moved. Idea taken from a separate implementation of this strategy that had the guard when this one did not.
- Added `detection.maxBookAgeCeilingMs`, a per-symbol freshness window that follows each symbol's own update cadence up to a cap. `bookTicker` pushes only on change, so a single global window judges a market that ticks twice a minute by the standard of one that ticks ten times a second - a measured Binance.US scan discarded 85% of its evaluations that way. Set to 0 to keep one window for every symbol; the executor still prices real orders against the strict window.
- Added a cycle-length sweep and a cost ranking to `symbols`. Cycles are now listed cheapest-first by the notional they need to clear their own lot grid, marked when they fit the configured `maxNotionalPerCycle`, and the count of cycles available at `maxCycleLength` 4 is reported alongside the configured length.
- Changed `detection.maxBookAgeMs` to 6000 in the Binance.US profile. `bookTicker` pushes only on change, so on a thin venue an untouched quote is accurate rather than stale, and a 1500ms window declines to price most cycles without saying so.
- Added an order-validation check to `doctor`: a real leg-1 order is sent to `POST /api/v3/order/test`, so the signature and every exchange-side filter are proven against the live venue without placing anything. Previously the signed order path was first exercised by the first order that spent money.
- Added a dust estimate to `symbols`: per cycle, the value left behind by lot rounding and the notional at which it equals the configured edge threshold. It needs no credentials and no funds, so a venue's viability can be checked before an account is funded.
